{% extends 'BDSubberBundle::layout.html.twig' %}

{% block title %}{{ parent() }} :: Dashboard{% endblock %}

{% block body %}
    <h1>Dashboard</h1>

    <div class="row-fluid">
        {% for item in items %}
            {{ render(controller('bd_subber.release_controller:showReleaseByNameAction', {'releaseName': item.originalName})) }}
        {% endfor %}
    </div>
{% endblock %}
